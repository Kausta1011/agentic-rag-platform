# This project stores env variables in the `.env/` FOLDER, not a single
# `.env` file. Copy `.env/variables.env` and fill in the keys there.
#
# See README > "Quick start" and the commentary at the top of
# `.env/variables.env` for the full list of knobs.
